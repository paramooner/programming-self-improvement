# 数学建模作图规范（全队约定）

> 目的：让全队所有图"看起来是一套"，达到国奖论文的及格线以上。
> 原则：**先保证"论文级"（矢量、字号、编号、单位），再谈美观。**

---

## 1. 硬性规范（不满足=扣分）

| 项目 | 约定 |
|---|---|
| 图题 | **图下方**居中，格式 `图N 标题`（如 `图1 技术路线图`），全文连续编号，正文引用时必须出现 |
| 表题 | **表上方**居中，格式 `表N 标题` |
| 坐标轴 | 必须标注**变量名 + 单位**（如 `时间 (年)`、`TFR`），无量纲也要写清含义 |
| 编号 | 图/表独立编号，与正文引用一一对应，不允许有"图5"却在正文找不到的情况 |
| 图内文字 | 字号不小于正文的约 70%（正文小四 12pt → 图内 ≥ 9pt），保证 Word 缩印后仍可读 |
| 输出 | 优先 **PDF 矢量**；位图必须 ≥ 300 dpi 且 `bbox_inches='tight'` |
| 禁止 | 禁止直接截图 Excel/软件界面（锯齿+水印）、禁止拉伸变形（插入 Word 时锁定纵横比） |

## 2. 字体规范

- 中文：**黑体 SimHei**（图内文字用黑体，比宋体清晰）；正文排版用宋体（那是 Word 的事）
- 西文/数字：**Times New Roman**（与宋体搭配是中文论文惯例）
- Matplotlib 统一设置：
  ```py
  plt.rcParams['font.sans-serif'] = ['SimHei', 'Times New Roman']
  plt.rcParams['axes.unicode_minus'] = False   # 负号正常显示
  ```
- 图题字号：五号（10.5pt）加粗；坐标轴标签 ≥ 10pt；刻度 ≥ 9pt

## 3. 配色规范（全篇统一色板）

固定使用下列 **Okabe-Ito 色盲友好色板**（8 色），按顺序取用，不要混入其他颜色：

```
主色板（按使用优先级）:
  #0072B2  蓝（主色调，默认第一根线）
  #E69F00  橙
  #009E73  绿
  #D55E00  红
  #CC79A7  紫
  #56B4E9  天蓝
  #F0E442  黄
  #999999  灰（辅助/基准线）
```

补充约定：
- 基准线/无效假设线：灰色虚线 `#999999`，`ls='--'`，`alpha=0.6`
- 区间阴影：取同系色，`alpha=0.1~0.15`，低调不抢主线
- 强调某条线：加粗（lw=3）并保持同色系
- 一个图内颜色 ≤ 5 种，多系列用线型/点型辅助区分，不要只用颜色

## 4. 风格统一（二选一，全队一致）

模板库提供两套主题，选一套后全队都用它：

**A. 期刊风（journal）**：刻度向内、四边刻度、虚线网格、外框保留 —— 适合模型结果图、敏感性分析图
**B. 极简风（minimal）**：隐藏上/右边框、仅水平网格线、无框 —— 适合数据探索图、时间序列图

共同约定：
- 网格：浅灰 `alpha=0.3`，只显示水平线（数据图）或双轴虚线（期刊风）
- 图例：`frameon=False`（无边框），位置不遮挡数据
- 线宽：普通线 2，强调线 3，基准虚线 1.5~2
- 坐标轴范围：`set_xlim/set_ylim` 留 5~10% 余量，防止标注文字被裁切

## 5. 尺寸与命名规范

- 单幅图：`figsize=(8, 5)`，dpi=300
- 拼版多子图：`figsize=(12, 4~5)`，子图按 (a)(b)(c) 编号，字号可略小但 ≥ 8pt
- 出图时按**目标 Word 版心比例**设定 figsize，避免插入后变形
- 文件命名：`图N_短描述.png/pdf`（如 `图3_灵敏度分析.pdf`），统一存放 `figures/` 目录
- 同一图同时输出 PDF（排版用）和 PNG（预览/交流用）

## 6. 图的质量检查清单（提交前逐张过）

- [ ] 图题在下方、有编号、正文有引用
- [ ] 坐标轴有变量名和单位
- [ ] 中文无乱码（豆腐块）、负号正常
- [ ] 无糊图（PDF 矢量或 300dpi+）、无拉伸变形
- [ ] 颜色来自统一色板，图例无边框不遮数据
- [ ] 图内字号 ≥ 9pt，无文字截断
- [ ] 与正文结论一致（图要"说话"，不是摆设）

## 7. 快速出图工作流

```
写代码 → 打开/复制模板库对应图文件（改数据、标题）→ 输出 PDF+PNG 到 figures/
→ 按检查清单过一遍 → 插入 Word
```

赛时 90% 的图都应从模板库直接生成，只改数据和标题。
